// Code generated by tygo. DO NOT EDIT.
import { BaseModel } from './user-models';

//////////
// source: commitment.go

/**
 * ScheduleCommitment is the API-facing DTO for a schedule commitment.
 * A commitment links a period to a workout log. It is created first
 * (with workoutLogId=null) and linked to a WorkoutLog when the period activates.
 */
export interface ScheduleCommitment extends BaseModel {
  periodId: number /* uint */;
  date?: string;
  workoutLogId?: number /* uint */;
}

//////////
// source: period.go

/**
 * ScheduleType determines how commitments within a period are interpreted.
 */
export type ScheduleType = string;
/**
 * ScheduleTypeFixedDate means the user picks specific days within the period.
 * Each commitment has a Date set.
 */
export const ScheduleTypeFixedDate: ScheduleType = 'fixed_date';
/**
 * ScheduleTypeFrequency means the user sets a count (N commitments per period)
 * without picking specific days. Commitments have Date=null.
 */
export const ScheduleTypeFrequency: ScheduleType = 'frequency';
/**
 * PeriodStatus is the computed lifecycle state of a period, derived from dates.
 */
export type PeriodStatus = string;
/**
 * PeriodStatusPlanned means the period hasn't started yet (now < periodStart).
 */
export const PeriodStatusPlanned: PeriodStatus = 'planned';
/**
 * PeriodStatusActive means the period is currently in progress (periodStart ≤ now < periodEnd).
 */
export const PeriodStatusActive: PeriodStatus = 'active';
/**
 * PeriodStatusArchived means the period has ended (now ≥ periodEnd).
 */
export const PeriodStatusArchived: PeriodStatus = 'archived';
/**
 * PeriodMode determines how the period duration is computed when cloning.
 */
export type PeriodMode = string;
/**
 * PeriodModeNormal clones the period with the same duration in days.
 * Next period starts the day after the previous one ended.
 */
export const PeriodModeNormal: PeriodMode = 'normal';
/**
 * PeriodModeMonthly clones the period by advancing one calendar month.
 * The day-of-month offset is preserved (e.g. 4th Jan → 4th Feb).
 */
export const PeriodModeMonthly: PeriodMode = 'monthly';
/**
 * SchedulePeriod is the API-facing DTO for a schedule period.
 */
export interface SchedulePeriod extends BaseModel {
  scheduleId: number /* uint */;
  periodStart: string;
  periodEnd: string;
  type: ScheduleType;
  mode: PeriodMode;
  status: PeriodStatus; // derived, not stored
}

//////////
// source: schedule.go

/**
 * WorkoutSchedule is the API-facing DTO for a workout schedule.
 */
export interface WorkoutSchedule extends BaseModel {
  owner: string;
  workoutId: number /* uint */;
  startDate: string;
  endDate?: string;
  initialStatus: string;
  timezone: string;
  active: boolean; // derived, not stored
}
